# GPT-SoVITS 训练服务环境变量配置示例
# 复制此文件为 .env 并根据需要修改

# 基础路径配置
GPT_SOVITS_BASE_PATH=~/workspace/git/GPT-SoVITS

# 服务配置
SERVICE_HOST=0.0.0.0
SERVICE_PORT=8000
SERVICE_WORKERS=1
SERVICE_LOG_LEVEL=info

# 工作目录配置
WORK_DIR_NAME=api_tasks

# 预训练模型路径（相对于基础路径）
BERT_DIR=GPT_SoVITS/pretrained_models/chinese-roberta-wwm-ext-large
CNHUBERT_DIR=GPT_SoVITS/pretrained_models/chinese-hubert-base
PRETRAINED_SV=GPT_SoVITS/pretrained_models/sv/pretrained_eres2netv2w24s4ep4.ckpt

# 默认训练配置
DEFAULT_BATCH_SIZE=16
DEFAULT_EPOCHS_S2=50
DEFAULT_EPOCHS_S1=15
DEFAULT_GPU_ID=0
DEFAULT_LANGUAGE=zh